import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.scss'],
})
export class AppComponent {
  title = 'Barbearia App'; // Título da aplicação

  constructor() {
    // Lógica inicial do componente (se necessário)
  }
}
